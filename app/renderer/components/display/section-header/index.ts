import { SectionHeaderBody } from './section-header-body';
import { SectionHeaderLeading } from './section-header-leading';
import { SectionHeaderRoot } from './section-header-root';
import { SectionHeaderTrailing } from './section-header-trailing';

export const SectionHeader = {
  Root: SectionHeaderRoot,
  Leading: SectionHeaderLeading,
  Body: SectionHeaderBody,
  Trailing: SectionHeaderTrailing,
};

export { SectionHeaderRoot, SectionHeaderLeading, SectionHeaderBody, SectionHeaderTrailing };
