{
  "name": "@recast/ndi-native",
  "version": "0.1.0",
  "private": true,
  "description": "Native NDI sender bridge for Recast",
  "main": "index.js",
  "types": "index.d.ts",
  "gypfile": false,
  "scripts": {
    "build": "node-gyp rebuild",
    "clean": "node-gyp clean",
    "rebuild": "npm run clean && npm run build"
  },
  "dependencies": {
    "node-addon-api": "^8.3.1"
  },
  "devDependencies": {
    "node-gyp": "^12.1.0"
  }
}
